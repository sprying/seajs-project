#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-md5/bin/grunt-md5" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-md5/bin/grunt-md5" "$@"
  ret=$?
fi
exit $ret
